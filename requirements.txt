pycryptodome>=3.19,<4
zstandard>=0.22,<1
mcp>=1.0,<2
tzdata>=2024.1; sys_platform == "win32"
# uvicorn is only required when network.enabled=true (mcp_server.py SSE/streamable-http transport);
# if you only use stdio mode, you can skip installing it.
uvicorn>=0.30
